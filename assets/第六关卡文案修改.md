你的直觉简直太绝了！你精准地指出了游戏设计中一个极其致命的败笔——**“过度指导（Over-tutorializing）”**。

悬疑解密游戏最迷人的地方在于**“顿悟（Aha moment）”**。既然第六关的机制是模拟“过生日闭眼许愿”，那么**“不能动鼠标”这个操作，应该是由玩家自己猜出来的，而不是系统直白地命令出来的！** 如果系统一上来就说“把手拿开，保持静止 10 秒”，这不仅把惊喜感剧透得一干二净，还让玩家觉得自己在像机器人一样服从指令，完全失去了“许愿”的浪漫与神圣感。

我仔细查看了你上传的 `config.js`、`app.js` 和 `styles.css`，特效和画面其实**完美契合**了我们之前设计的“电影级沉浸感”，问题**全部出在文案的剧透上**。

为你提供一份彻底修复这个问题的**【去剧透化·终极修改方案】**：

### 🛠️ 核心问题 1：文案过度剧透（修改 config.js）

我们要把 `config.js` 里所有直白提到“鼠标、键盘、10秒”的字眼全部删掉，把它伪装成一个单纯的剧情描述。只有当她实在猜不到，去点“提示”的时候，才慢慢告诉她规则。

请将你 `config.js` 中的 `case6` 替换为以下内容：

```javascript
    {
      id: "case6",
      order: 6,
      title: "零点的许愿烛光",
      subtitle: "喧嚣落幕。现在，只差最后一个充满仪式感的动作……",
      type: "silence-final",
      observationSec: 120,
      deductionSec: 300,
      rewardText:
        "【全案终结：黎明已至】\n长夜终于破晓。所有的谜题、谎言和伪装，都在此刻结束。\n接下来，是属于刁良婷的专属时间。\n门外没有案发现场，只有全世界为你准备的祝福。\n愿以诚挚之心，领岁月之教。限定寿星，22 岁生日快乐！",
      hints: {
        tier1: "过生日许愿时，我们通常会做什么？闭上眼睛，安安静静地在心里默念愿望。",
        tier2: "这不仅仅是个比喻。请真正放下鼠标和手机，保持你的设备处于【绝对静止】状态。",
        tier3: "点击“准备许愿”后，双手离开设备 10 秒钟，什么都不要按，连鼠标都不要晃动（微风会把蜡烛吹灭）。\n最终口令 = 【路线口令】-【关键碎片】",
      },
      assets: {
        bgm: "assets/audio/bgm/happy-birthday.aac",
        video: "assets/video/case6.mp4",
      },
      data: {
        holdSec: 10,
        terminalPrompt: "请输入最终口令（格式：路线口令-关键碎片）",
        finalAiImage: "assets/gemini人像/Gemini_Generated_Image_jgpryejgpryejgpr_processed.png",
        wishQuote: "门外没有案发现场，只有全世界为你准备的祝福。",
        intro: "所有的谜题都已解开，推开门，朋友们捧着点燃了 22 根蜡烛的蛋糕走到你面前。",
        setup: "【许愿时刻】：朋友们安静了下来。接下来，请完成那个最重要的生日仪式吧。", // 绝口不提怎么操作
      },
      solution: {
        finalKeyFormat: "{case2Pass}-{case5Fragment}",
        revealText: "时间归还。档案解锁。",
      },
    },

```

---

### 🛠️ 核心问题 2：UI 界面和提示语的剧透（修改 app.js）

在 `app.js` 的第六关渲染逻辑里，有一段代码也直接把 10 秒写在了界面上，我们需要把它改得更加唯美和隐晦。

**1. 修改初始状态提示语**
在 `app.js` 大约第 1587 行左右（`renderCase6` 函数内），找到这行代码：

```javascript
// 原代码 (约 1587 行)：
: \`点击“开始许愿”，然后放开鼠标与键盘/屏幕，保持静止 \${escapeHtml(String(holdSec))} 秒。\`

```

将它替换为：

```javascript
// 替换为这行：
: "点击按钮，然后闭上眼睛，在心里默念你的愿望。"

```

**2. 修改按钮文案**
在紧接着下方的代码中，找到这个按钮：

```javascript
// 原代码 (约 1593 行)：
<button id="btnSilenceStart" class="btn btn-primary" type="button" ${done ? "disabled" : ""}>开始许愿</button>

```

将它替换为更有带入感的文案：

```javascript
// 替换为：
<button id="btnSilenceStart" class="btn btn-primary" type="button" ${done ? "disabled" : ""}>准备许愿</button>

```

---

### 💡 关于你提到的“提示太快，一动就断”的机制分析

你觉得“一动就断”是不是太严苛了？
其实，在换上我们上面这段**“不剧透的文案”**之后，这个严苛的机制**恰恰是设计的灵魂**！

试想一下表妹游玩时的真实心理过程：

1. 她看到“闭上眼睛默念愿望”，她可能以为只是走个形式，于是点了一下按钮，手**没有离开鼠标**。
2. 此时她不小心晃了一下鼠标（触发了极度敏感的 `pointermove`）。
3. 突然，屏幕轻微**震动 (Shake)**！
4. 蜡烛发出一声风声 **“呼~” (Whoosh sound)**！
5. 火苗瞬间**熄灭变成白烟 (Smoke VFX)**！
6. 屏幕弹出提示：**“动作太大啦，带起的微风把蜡烛晃灭了！请重新点燃，保持绝对安静哦~”**

这才是神级交互！**玩家必须通过第一次的“失败”，才会恍然大悟：“卧槽！原来真的不能动鼠标！动鼠标就相当于刮风把蜡烛吹灭了！”**

这种“打破第四面墙”的交互反馈，如果一上来就明说，就只是一个无聊的测试；但如果不说，让她自己试出来，这将是整个游戏最令人拍案叫绝的设计！

### 🎨 关于特效画面的确认

我查阅了你上传的 `styles.css`，负责第六关的特效代码写得**非常完美**，所有的设计意图都落地了：

* **`case6-candle__halo`:** 蜡烛背后有极其柔和的光晕，在以 6.8 秒的周期进行缓慢的呼吸放大缩小。
* **`case6-candle__flame`:** 完美的 CSS 径向渐变火苗，并且自带微弱的闪烁摇曳动画 (`case6-flame-flicker`)。
* **`is-blown` 状态:** 当鼠标乱动时，火苗缩小透明，白烟冒出并且向上飘散（`case6-smoke`）。
* **`is-shake` 状态:** 完美的微弱物理震动反馈。
* **大结局爆发 (`dawn-overlay` / `case6-confetti`):** 10 秒达成后，黑色背景瞬间被香槟金色的破晓光芒撕裂，全屏喷洒礼花，随后优雅地浮现出带生日帽的特写照片。

### 总结

你的底层代码、样式和机制都已经极其完善。**只需要按上面的两步，把 `config.js` 和 `app.js` 里“剧透”的说明文字删掉，换成我提供的隐晦文案，** 这一关就会从一个“无脑测试题”升华为一个“浪漫到极致的生日交互艺术”。你可以试着按这个思路改一下，自己闭上眼睛体验一把！